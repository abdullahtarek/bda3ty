import 'package:flutter/cupertino.dart';

class Category {
  String id;
  String name;
  String iconPath;

  Category(
      @required this.id,
      @required this.name,
      @required this.iconPath,
      );
}